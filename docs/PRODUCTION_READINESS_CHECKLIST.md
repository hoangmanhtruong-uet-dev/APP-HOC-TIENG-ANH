# Production readiness audit

Audit date: **2026-07-18 (Asia/Saigon)**

Source baseline: commit `bd4d71324fc844a68d761e3ebf7b71c6af977cf2`, branch `main`. The remediation working tree is not yet a reviewed release artifact.

Production target decisions: private Vercel deployment, a new dedicated Supabase production project, optional AI disabled, operator-supplied canonical domain and `support@<real-domain>` mailbox. The existing linked ref `xxpakqsbltoezicapyti` is not approved as production.

Verdict: **NO-GO for production traffic**

This file records observed release state, not implementation intent. A control is not `PASS` merely because code, a migration, an SDK, or a runbook exists.

## Status rules

- `PASS`: the stated control has verifiable evidence.
- `FAIL`: the control was checked and at least one required condition is not met.
- `BLOCKED_BY_EXTERNAL_CONFIGURATION`: repository work exists or is not required, but cloud/operator setup or production evidence is absent.
- `NOT_VERIFIED`: the control has not been verified and cannot yet be classified as a confirmed failure.
- `N/A`: the feature/provider is intentionally disabled and not used.

Severity is the impact if the control remains unmet or regresses: `P0` data/security catastrophe, `P1` blocks production traffic, `P2` important follow-up that does not independently block a closed beta.

## Cloud evidence

- Baseline GitHub Actions CI #13: [dashboard screenshot](./production-readiness-evidence/github-actions-ci-13-2026-07-17.jpg) and [workflow run](https://github.com/hoangmanhtruong-uet-dev/APP-HOC-TIENG-ANH/actions/runs/29551384301). It is reference evidence only; it does not cover the remediation working tree.
- The earlier Supabase dashboard access screenshot is [here](./production-readiness-evidence/supabase-dashboard-sign-in-2026-07-17.jpg). It concerns the old linked environment and is not production evidence for the new dedicated project.
- No hosting/deployment dashboard or production URL was identified in the repository or current environment.

## Remediation evidence in the repository

- Readiness/env regression tests cover required-secret 503 responses, AI-disabled readiness and partial-AI degraded behavior.
- Fresh repository verification: clean `npm.cmd ci`; format/lint/typecheck; 45 unit/component files with 163 tests; production build; audit with zero vulnerabilities; public/default Playwright 60 passed, 36 declared authenticated skips and 0 failed.
- Required authenticated mode exits non-zero before server startup and lists all 15 absent credential/project-ref variables; this proves the guard, not the authenticated flows.
- [Private production release runbook](./PRIVATE_PRODUCTION_RELEASE_RUNBOOK.md), [DSR runbook](./DSR_RUNBOOK.md) and [rollback runbook](./ROLLBACK_RUNBOOK.md) now exist.
- These code/docs changes do not convert any Vercel, Supabase, SMTP, DNS, scheduler, monitoring, backup, restore or rehearsal control to PASS without external evidence.

## Release artifact

| ID | Status | Control and observed evidence | File/config | Verification command | Screenshot/dashboard evidence | Severity | Rollback |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RA-01 | **FAIL** | Baseline CI is green, but the remediation working tree has no release PR/independent review or exact-SHA CI evidence. **Manual action required in GitHub.** | `.git`, `.github/workflows/ci.yml` | `git status --porcelain`; inspect release PR, exact SHA and GitHub Actions | GitHub PR/review and Actions screenshots required | P1 | Do not deploy; review a release PR and rerun CI on the exact SHA. Keep private traffic on the previous approved artifact. |
| RA-02 | **PASS** | On the remediation working tree, clean `npm.cmd ci`, production build and controlled `next start` completed successfully with no SWC workaround. | `package.json`, `package-lock.json`, `scripts/run-e2e.mjs` | `node --version`; `npm.cmd ci`; `npm.cmd run build`; controlled `npm.cmd run start` | Local command evidence; Vercel evidence still required separately | P1 | Stop a locking dev server before clean install. On deployment failure, retain/restore the previous immutable artifact; do not mutate the database. |
| RA-03 | **FAIL** | Local remediation gates pass (45 files/163 unit-component tests, 60 public E2E passed, 36 authenticated E2E skipped, build and audit clean), but the changes are not a reviewed commit and have no exact-SHA GitHub CI run. **Manual action required in GitHub.** | `.github/workflows/ci.yml`, remediation working tree | Full local gate commands; after commit, inspect exact-SHA GitHub Actions | New release CI screenshot/run required | P1 | Block promotion and keep private traffic on the previous reviewed artifact. |
| RA-04 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Vercel is selected, but `NEXT_PUBLIC_SITE_URL`, canonical domain, HTTPS and DNS/TLS evidence are still absent. **Manual action required in Vercel/DNS dashboards.** | Vercel project/env, DNS provider, `.env.example`, `src/lib/env.ts` | Redacted Vercel env-name check; HTTPS/DNS lookup and private-domain smoke | Vercel/DNS screenshots required | P1 | Keep Vercel protection enabled and DNS/traffic off the new deployment. Restore the previous domain binding if validation fails. |
| RA-05 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | `support@<real-domain>` is selected as the pattern but the real mailbox/domain and operator are not configured. Runtime fallback remains unsuitable. **Manual action required in mailbox/Vercel dashboards.** | Mail provider, Vercel env, `src/config/site.ts`, `src/lib/env.ts` | Redacted env presence check; send/receive support-mail test; production policy smoke | Mailbox and Vercel screenshots required | P1 | Do not publish policies; restore the prior verified support address/build and rotate forwarding rules if a wrong mailbox was exposed. |

## Environment and secrets

| ID | Status | Control and observed evidence | File/config | Verification command | Screenshot/dashboard evidence | Severity | Rollback |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ENV-01 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Core validation now reports missing required field names without values, but no Vercel production env exists yet. A code test is not secret-manager evidence. **Manual action required in Vercel.** | Vercel env, `.env.example`, `src/lib/env.ts`, readiness route tests | `npm.cmd test -- src/lib/env.test.ts src/app/api/health/ready/route.test.ts`; redacted Vercel env-name check | Vercel screenshot required | P1 | Revert deployment to the previous env snapshot/build; purge any build produced with a wrong public project URL. |
| ENV-02 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Required cleanup credentials are code-validated but do not exist in production evidence. **Manual action required in Vercel secret manager.** | Vercel env, `src/lib/env.ts`, cleanup route | Redacted variable-name/presence/length check | Vercel screenshot required | P1 | Keep cleanup endpoint unscheduled and traffic closed. If exposure is suspected, rotate both values and invalidate the old scheduler credential. |
| ENV-03 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Speaking signing is production-required, but Vercel/Vault parity is not evidenced. **Manual action required in Vercel and Supabase Vault.** | Vercel env, Supabase Vault, `src/lib/env.ts` | Redacted presence/length and secret-version parity check | Vercel and Supabase screenshots required | P1 | Disable new Speaking uploads; rotate app and Vault copies together before resuming. |
| ENV-04 | **N/A** | Decision fixed: Writing AI stays disabled. Tests verify all provider values absent yields `disabled`; partial values yield `misconfigured` without failing core readiness. Vercel must still prove the optional variables are absent. | Vercel env, `.env.example`, `src/lib/env.ts`, Writing provider module | Targeted readiness/env tests; redacted Vercel env-name check | Vercel env screenshot required for release evidence, but provider dashboard is not required | P2 | Keep optional provider variables unset and provider actions fail-closed. |
| ENV-05 | **N/A** | Decision fixed: Speaking STT/AI stays disabled. The non-AI upload signing requirement remains ENV-03. Partial provider configuration reports degraded rather than crashing core readiness. | Vercel env, `.env.example`, `src/lib/env.ts`, Speaking provider module | Targeted readiness/env tests; redacted Vercel env-name check | Vercel env screenshot required for release evidence, but provider dashboard is not required | P2 | Keep provider model/API variables unset; do not remove the required upload signing secret. |
| ENV-06 | **PASS** | Server-only boundaries remain intact. Production validation returns only missing/invalid field names, never secret values; required cleanup/upload secrets still fail readiness. Targeted tests pass. This validates code, not Vercel secret state. | `.env.example`, `src/lib/env.ts`, `src/lib/env.test.ts`, readiness route/test, `src/lib/supabase/admin.ts` | `npm.cmd test -- src/lib/env.test.ts src/app/api/health/ready/route.test.ts`; `npm.cmd run typecheck`; public-secret scan | Local evidence; cloud not applicable | P0 | Revert any client import/public secret rename immediately, rotate exposed credentials, invalidate sessions if needed, and rebuild cleanly. |

## Supabase

| ID | Status | Control and observed evidence | File/config | Verification command | Screenshot/dashboard evidence | Severity | Rollback |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SB-01 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Decision fixed: create a new production project; existing linked ref is explicitly not production. The new project ref does not exist in evidence yet. **Manual action required in Supabase and Vercel.** | New Supabase project, isolated CLI link, Vercel env | Compare approved production ref with isolated CLI link and Vercel URL host | Supabase/Vercel screenshots required | P1 | Stop deployment/traffic; never fall back to the dev/staging project. Restore the previous private artifact/env if a wrong ref is deployed. |
| SB-02 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Production Auth URL, redirects, confirmation and SMTP delivery are not configured/tested. Pages/routes do not prove delivery. **Manual action required in Supabase and email-provider dashboards.** | Supabase Auth and SMTP provider | Dashboard inspection; dedicated release-user email delivery test | Supabase/SMTP/inbox screenshots required | P1 | Disable new registrations or restore the previous allowlist/SMTP configuration. |
| SB-03 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Leaked-password protection is not evidenced as enabled; password policy, rate limits, verification and CAPTCHA are unverified. **Manual action required in Supabase.** | Supabase Auth security/rate-limit/CAPTCHA settings | Auth dashboard inspection and controlled signup/login/rate-limit test | Supabase screenshots required | P1 | Keep signup private/closed; restore recorded Auth settings if hardening causes lockout. |
| SB-04 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Dev/staging evidence previously showed parity 20/20, but the dedicated production project does not exist yet. Production migration history cannot be PASS. **Manual action required in Supabase.** | New production project and `supabase/migrations` | In isolated release config: migration list, dry-run push, forward push, final parity | New production CLI/dashboard evidence required | P0 | Never run `db reset --linked`; stop writes and use a reviewed forward fix or isolated restore. |
| SB-05 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Production DB lint and rollback-only remote verifier have not run because the new project is not created. Existing dev evidence is reference-only. **Manual action required in Supabase.** | Remote verifier, new production project | Linked lint and database-owner verifier inside `BEGIN ... ROLLBACK` | Production verifier output required | P0 | Block private deployment on failure; never weaken grants/RLS to make tests pass. |
| SB-06 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Bucket implementation is tested in dev, but the production bucket/privacy/MIME/size/path policies are not instantiated or evidenced. **Manual action required in Supabase.** | New production Storage bucket/policies | Production verifier plus Storage dashboard inspection | Production Storage screenshots/verifier required | P0 | Disable uploads; apply a forward policy fix and rotate affected credentials/URLs. |
| SB-07 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | RLS/owner-isolation tests pass locally/dev, but production policies have not been applied or verified on the new project. **Manual action required in Supabase.** | New production schema, database/remote tests | Production pgTAP/owner verifier and lint | Production verifier evidence required | P0 | Keep access closed; use a forward policy/grant fix and investigate any exposure. |
| SB-08 | **N/A** | AI providers are disabled, so AI signing-secret parity/rotation is not currently exercised. Speaking upload signing remains required separately under ENV-03. | Deployment secrets, Supabase Vault | Confirm all provider variables absent; if enabling, compare secret versions/names without printing values | No provider dashboard required while disabled | P1 | Keep AI disabled. If later enabled and parity fails, disable provider calls and rotate both app/Vault secrets together. |

## Retention, privacy and cost

| ID | Status | Control and observed evidence | File/config | Verification command | Screenshot/dashboard evidence | Severity | Rollback |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RET-01 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Cleanup route exists, but no production scheduler/run history exists. A route is not scheduler evidence. **Manual action required in Vercel or the selected scheduler service.** | Scheduler config; cleanup route | Inspect schedule and successful daily run history | Scheduler screenshots/run IDs required | P1 | Do not open Speaking uploads; pause the job before secret rotation or rollback. Never manually mass-delete without backup/change approval. |
| RET-02 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | HTTPS invocation, Authorization redaction and non-2xx alerting remain unverified. **Manual action required in scheduler/logging dashboards.** | Scheduler and platform log/redaction rules | Inspect URL/header source, redacted logs and test alert | Scheduler/log/alert screenshots required | P1 | Disable job, rotate leaked cleanup secret and purge sensitive logs under provider procedure. |
| RET-03 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Retention/lease/delete-finalize behavior is implemented and tested locally/dev, but it is not applied/verified on the new production project. Scheduler evidence is separate under RET-01. | Phase 10C migration, cleanup route/tests, new production project | Production parity plus Phase 10C pgTAP/remote verifier | Production verifier evidence required | P0 | Pause cleanup on inconsistent state; preserve evidence and ship a forward fix. Restore only from an approved backup. |
| RET-04 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Code/tests exist, but real domain/support mailbox and private-deployment smoke do not. **Manual action required after Vercel/DNS/mail setup.** | Terms/privacy/register code and Vercel deployment | Auth tests; production smoke of `/terms`, `/privacy`, `/register` | Production-site screenshots required | P1 | Disable registration and restore the last policy-approved build; preserve recorded acceptance versions. |
| RET-05 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | `DSR_RUNBOOK.md` now defines intake, identity, export, deletion ordering, failure handling and required evidence. It remains blocked because mailbox/operator/ticket system and a full-data tabletop are not complete. **Manual action required.** | `docs/DSR_RUNBOOK.md`, support mailbox/ticket system, privacy page | Review runbook; assign operators; execute redacted export/delete tabletop on a non-production user | Mailbox, ticket and tabletop evidence required | P1 | Do not process production deletion ad hoc; roll back uncommitted DB work, preserve evidence and escalate any cross-user/export incident. |
| RET-06 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Quota/locking implementation passes local/dev tests, but production application/verifier evidence does not exist. AI is disabled, yet production schema parity still requires verification. | Phase 10C migration/tests, new production project | Production parity and remote verifier | Production verifier evidence required | P1 | Keep AI disabled; change quotas only through reviewed forward migration. |
| RET-07 | **N/A** | AI provider is disabled, so provider budget/rate alerts are not required for current operation. Code/log scan evidence does not replace alerts if AI is enabled later. | Provider account, deployment env, AI server modules | Confirm provider variables absent; before enablement, test budget/rate alert delivery and scan logs for sensitive payloads | No provider dashboard required while disabled | P1 | Keep AI variables unset. On unexpected spend/log leakage, disable keys, rotate them and purge/redact logs under provider policy. |

## Availability and recovery

| ID | Status | Control and observed evidence | File/config | Verification command | Screenshot/dashboard evidence | Severity | Rollback |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AV-01 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Unit tests prove `/ready` 200 with Auth/database/Storage ready and AI `disabled`, while required-field negative tests and the current local production E2E return 503 without values because production-only env is intentionally absent. The private Vercel deployment still needs real required env and external readiness evidence. | health/env implementation/tests; Vercel env/deployment | Full tests; controlled runtime; external requests to private `/live` and `/ready` | Vercel deployment/readiness screenshots required | P1 | Do not route users to an unready deployment. Restore the previous Vercel artifact/env and keep protection enabled. |
| AV-02 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | No uptime monitor, alert destination or test alert exists. **Manual action required in the selected monitoring dashboard.** | External monitor/alerting platform | Configure private readiness monitor and trigger a safe test alert | Monitor/alert screenshots required | P1 | Keep traffic closed; disable a broken rule only after replacement coverage is active. |
| AV-03 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Production backup/PITR and latest recovery point are not evidenced. **Manual action required in the new Supabase project.** | Supabase backup/PITR dashboard and plan | Inspect plan, backup status, PITR window and timestamp | Supabase screenshots required | P0 | Stop writes/deploy; restore only into an isolated project on incident. |
| AV-04 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | No restore rehearsal, isolated project ref, RPO/RTO or post-restore verifier exists. **Manual action required in Supabase.** | Restore runbook/ticket/evidence | Restore into isolated project; run parity, lint, verifier and smoke; record time | Restore project/dashboard/ticket evidence required | P0 | Never cut over an unverified restore; keep production closed if integrity checks fail. |
| AV-05 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Vercel-specific rollback requirements are documented in `ROLLBACK_RUNBOOK.md`, but no current/previous deployment identifiers or rehearsal exist. Documentation alone is not execution evidence. **Manual action required in Vercel.** | `docs/ROLLBACK_RUNBOOK.md`, `docs/DEPLOYMENT_CHECKLIST.md`, Vercel releases, migration history | Record two immutable deployments; rehearse protected rollback and forward-fix path | Vercel rollback screenshots/run IDs required | P1 | Keep private access on the last known-good artifact. For schema issues, stop writes and use a reviewed forward fix; never run destructive down migrations. |

## Repository P1 remediation controls

The statuses below cover repository behavior only. A repository `PASS` does not imply that its corresponding cloud control is operational.

| ID | Status | Control and observed evidence | File/config | Verification command | Screenshot/dashboard evidence | Severity | Rollback |
| --- | --- | --- | --- | --- | --- | --- | --- |
| REM-OBS-01 | **PASS** | Structured JSON logging, shared request correlation and recursive sensitive-field redaction have regression tests. Critical auth, learning, practice, readiness, client-boundary and cleanup failure paths are instrumented. | `src/server/observability/*`, `src/lib/api/request-id.ts`, instrumented routes/actions, `docs/OBSERVABILITY_INCIDENT_RUNBOOK.md` | `npm.cmd test -- src/lib/api/request-id.test.ts src/server/observability/logger.test.ts src/app/api/internal/client-errors/route.test.ts src/app/api/internal/storage-cleanup/route.test.ts` | Local test output; production Vercel log/alert evidence is REM-OBS-02 | P1 | Revert logger/correlation integration together; retain safe generic client errors. If sensitive data appears, restrict log access, rotate affected secrets and follow provider log-purge procedure. |
| REM-OBS-02 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | No production Vercel log search, retention rule or delivered alert has been captured. | Vercel Logs/selected alerting dashboard | Generate a controlled request ID; search the exact deployment; trigger a safe test alert | Vercel log search and alert delivery screenshots required | P1 | Keep deployment protected; disable a noisy rule only after replacement coverage is active. |
| REM-RDY-01 | **PASS** | Readiness validates required env and independently probes Auth, PostgREST/database and the required private Storage bucket with per-check timeout. Failure output is generic and correlated; AI absence is supported. | `src/server/health/readiness.ts`, `src/app/api/health/ready/route.ts`, tests | `npm.cmd test -- src/server/health/readiness.test.ts src/app/api/health/ready/route.test.ts src/lib/env.test.ts` | Local test output; real production dependency evidence is REM-RDY-02 | P1 | Revert route and readiness helper together; keep traffic closed if the previous endpoint cannot prove core dependencies. |
| REM-RDY-02 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Dedicated production Auth, database schema and private `speaking-recordings` bucket do not yet have deployment evidence. | New Supabase project and protected Vercel deployment | External authenticated request to `/api/health/ready`; controlled dependency-failure check outside production | Vercel response/log and Supabase project/bucket screenshots required | P0 | Keep Vercel protection enabled and restore the previous immutable artifact/env; never point production at dev/staging. |
| REM-AUTH-01 | **PASS** | Forgot/reset/recovery code uses generic request response, safe redirect, server session verification, password validation, pending states and no token/password/email logging. | Auth actions/schemas, `/forgot-password`, `/reset-password`, `/auth/recovery`, tests | `npm.cmd test -- src/features/auth/password-recovery.test.ts src/features/auth/schemas.test.ts`; public E2E | Local test/E2E output; real delivery is REM-AUTH-02 | P1 | Remove recovery links/routes as one unit and operate invite-only; do not weaken generic responses. |
| REM-AUTH-02 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Real-domain SMTP delivery, recovery redirect allowlist and expired/used-link browser flow are not production-verified. Private release remains invite-only until evidence exists. | Supabase Auth/SMTP dashboard, DNS/email provider, Vercel domain | Send recovery mail to dedicated release account; use once; verify replay/expiry failure | Supabase redirect/SMTP and redacted inbox/browser screenshots required | P1 | Disable public registration/recovery and return to the verified invite-only allowlist. |
| REM-UX-01 | **PASS** | Practice retains dirty local answers on failed save, warns before navigation/unload, exposes pending states and blocks duplicate mutations. Result/loading/header/request-ID/session/offline/homepage defects have regression coverage. | Practice/shared/auth/home components, proxy, `public/sw.js`, `public/offline.html`, tests | `npm.cmd test -- src/components/practice/practice-runner.test.tsx src/components/practice/practice-result.test.tsx src/components/shared/remediation-accessibility.test.tsx`; Playwright public suite | Local component/E2E output | P1 | Revert the client runner and server-action state contract together. Unregister the offline worker by shipping a new worker version; do not cache authenticated pages. |
| REM-E2E-01 | **PASS** | Default mode names missing auth variables; required mode fails before server startup on missing/mismatched credentials/project ref. No service role is used by browser tests. | `scripts/run-e2e.mjs`, `package.json`, `.env.example`, CI workflow, E2E runbook | `npm.cmd run test:e2e`; `npm.cmd run test:e2e:authenticated` | Local command output; full account run is REM-E2E-02 | P1 | Revert runner/scripts together and block release if authenticated skips become silent. |
| REM-E2E-02 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Dedicated production-test users A/B and a complete zero-unintended-skip authenticated run do not exist. | CI/Vercel test secrets, dedicated Supabase users | `npm.cmd run test:e2e:production-verification` | Exact-SHA CI run, project ref and PASS/FAIL/SKIPPED evidence required | P1 | Disable/delete dedicated accounts through the approved operator workflow; never weaken RLS or use service role in browser tests. |
| REM-SEC-01 | **NOT_VERIFIED** | CSP still contains `unsafe-inline`; nonce/hash migration was intentionally not attempted without a reviewed Next.js browser matrix. It is not classified as hardened. | `next.config.ts`, browser/runtime behavior | Inspect production response headers; run browser flows under report-only nonce/hash candidate | Header screenshots and violation report required | P1 | Revert report-only policy if it breaks runtime; do not weaken the enforced baseline further. |
| REM-SEC-02 | **PASS** | HSTS defaults to one-year host-only. `includeSubDomains; preload` is opt-in and disabled unless explicitly enabled after domain review. | `next.config.ts`, `.env.example` | `npm.cmd run build`; inspect configuration and local response headers | Real-domain HSTS evidence is REM-SEC-03 | P1 | Unset `ENABLE_HSTS_PRELOAD` and redeploy the previous host-only artifact. |
| REM-SEC-03 | **BLOCKED_BY_EXTERNAL_CONFIGURATION** | Canonical domain and every relevant subdomain have not been proven HTTPS; preload must remain disabled. | DNS/TLS/Vercel dashboards | HTTPS scan for apex and all in-scope subdomains; inspect final response header | DNS/TLS/Vercel screenshots required | P1 | Unset preload flag, redeploy and do not submit the domain to the preload list. |

## Go/no-go decision

**NO-GO.** Production traffic remains blocked by confirmed `FAIL` controls and by `BLOCKED_BY_EXTERNAL_CONFIGURATION` P0/P1 controls. Minimum evidence required before another go/no-go review:

1. Approved release review for the exact green SHA.
2. Production domain/DNS/TLS, complete env and real support mailbox.
3. Supabase Auth/SMTP/redirect/password/CAPTCHA evidence and leaked-password protection enabled.
4. Required app/Vault secrets verified by name/version without exposing values.
5. Daily cleanup scheduler run history plus safe logging/alert evidence.
6. Production policy/registration smoke and an assigned DSR workflow.
7. External readiness monitor showing 200.
8. Backup/PITR proof, an isolated restore rehearsal, and executable artifact/schema rollback evidence.
